<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="icon" type="image/png" href="{{ asset('build/images/treebz.png') }}"/>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

        <title>
            {% block title %}Treeb'Z | PDF
            {% endblock %}
        </title>
        <style>
            @import url('https://fonts.googleapis.com/css2?family=Chewy &display=swap');

            .full-card {
                position: relative;
            }

            .member-name {
                position: relative;
                color: #212529;
                font-family: 'Chewy', cursive;
                z-index: 1;
            }

            .member-photo {
                position: relative;
                z-index: -1;
            }

            .card-number {
                display: flex;
                justify-content: center;
                position: relative;
                margin-left: 160;
                z-index: 1;
                border-radius: 100%;
                color: #212529;
                height: 35px;
                width: 35px;
                text-align: center;
            }

            .number {
                display: inline-block;
            }

            .theme {
                height: 8.6cm;
                width: 6.8cm;
                z-index:0;
            }

            .all {
                height: 8.6cm;
            }

            .photo {
                height: 5.16cm;
                width: 5.77cm;
            }
        </style>
    </head>
    <body>
        <div class="full-card">
            {% for theme in command.selectedThemes %}
                <div class="container-fluid row">
                    {% for member in command.members %}
                        <div class="all">
                            <img class="theme" src="{{ absolute_url(asset('/uploads/' ~ theme.image)) }}" alt="card"/>
                            <div class="member-photo">
                                <img class="photo" src="{{ absolute_url(vich_uploader_asset(member)) }}" alt="{{ member.picture }}"/>
                            </div>
                            <div class="member-name">
                                <p>{{ member.name }}</p>
                            </div>
                            <div class="">
                                <div class="card-number">
                                    <span class="number">{{ loop.index }}</span>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endfor %}
        </div>
    </body>
</html>
